# SQLite database (Milestone 1). Swap to a Postgres connection string for M4.
DATABASE_URL="file:./dev.db"

# Admin views (/admin?key=...). CHANGE THIS before any public deploy.
ADMIN_KEY="local-admin"
