# Insight Principles — loop log

A lightweight, round-by-round record of the INSIGHT PRINCIPLES loop: what each
round strengthened, what it deferred, and — importantly — when a round
**converged** with no change. The constitution's own amendment log
(`INSIGHT-PRINCIPLES.md`) records only actual amendments; convergence rounds
and deferrals live here so the amendment log stays a clean history of real
changes.

The loop's discipline: *do not make changes for the sake of activity.* A round
that finds no high-confidence, principle-aligned improvement is a successful
round — it records the assessment and stops.

---

## 2026-07-04 · Conviction-4 propagation (substantive)

**Strengthened.** A parallel 6-surface audit converged on one gap: conviction 4
("Round to what you'd defend in a hallway") is deliberately two-sided — default
to rounding, but reserve the exact digit for when it *is* the point (a covenant,
a threshold, a reconciliation) — yet three surfaces had flattened it to "rounded
always wins." Restored the carve-out in `house-view.ts` (the quantification
stance), `lessons.ts` (the quantification micro-lesson), and `generate.ts`
(quantification was the only conviction missing from the "make the changed
attribute LOUD" list). Also fixed a self-contradiction in the constitution's
worked example ("every failure in the taxonomy below" → "most of the craft
failures below" — the ninth failure, the exceedance, is one a strictly-faithful
telling cannot commit; the fix tightens the craft/fidelity wall).

**Deferred.** A standalone generator/reviewer checklist section (each conviction
already carries a "*Great insight: …*" checklist line; a separate section would
restate them and add a drift surface). An audience flip for `data_leader`/`other`
(would need a conviction about how they read + a matching lessons coda; not
high-confidence). Anything touching the over-reached so-what (borders fidelity;
unsafe on a client surface).

**Commit.** `insight: propagate conviction 4's load-bearing-digit carve-out
across the surfaces`.

## 2026-07-04 · Re-audit (converged — no change)

**Assessment.** Re-audited all four source-of-truth surfaces and the
constitution against the freshly-propagated principles. No new high-confidence,
principle-aligned change rises above low-value churn:

- `ATTRIBUTES.md` (the one surface a malfunctioning auditor skipped the prior
  round) was audited directly: it is the *mechanical* tagging rubric, and every
  section maps cleanly to its conviction. Its quantification rules correctly
  carry **no** load-bearing-digit carve-out — that is a taste judgment belonging
  to the WHY doc, not the mechanical HOW; adding it would be a category error and
  create principle-vs-rule drift, not remove it.
- `house-view.ts`, `lessons.ts`, `generate.ts` — the conviction-4 carve-out is
  present and consistent; every stance/lesson still maps to its conviction; no
  surface leaks fidelity vocabulary.
- The constitution is structurally complete (thesis, five convictions, worked
  example, failure taxonomy, shape→device table, audience flip, blinded internal
  note) and synchronized with its surfaces.

**Decision.** Converge. No edits to the four surfaces or the constitution this
round.

**What would re-open the loop.** New study data that overrules a desk stance
with a clean interval (amend the overruled conviction); a new craft attribute or
device entering the taxonomy; the weekly INSIGHT SYNTHESIS loop surfacing a
durable aggregate pattern worth encoding; or a genuinely sharper distinction /
example that clears the high bar for touching the constitution.

## 2026-07-04 · Re-check (still converged — state unchanged)

Loop re-fired. Verified via git that the four source-of-truth surfaces
(`house-view.ts`, `ATTRIBUTES.md`, `generate.ts`, `lessons.ts`) and the
constitution are **untouched since the prior convergence** — the intervening
commits were all teaching / design / perf-doc / deploy-ops work that never
touches an INSIGHT surface or introduces a new craft principle. None of the
re-open triggers above has fired. A fresh look at the standing deferrals (a
"clarity" dimension — already carried by the thesis's "how much of the number's
real information survives"; a generator/reviewer checklist — redundant with the
convictions' own "*Great insight: …*" lines; a `data_leader`/`other` audience
flip — speculative; the over-reached so-what — borders fidelity) still finds
none clearing the bar. No change.

**Standing policy to avoid churn:** while the four surfaces + constitution
remain unchanged since this note **and** no re-open trigger has fired, this loop
is a no-op — re-checking is fine, but do not re-log or edit for its own sake.
The next entry here should coincide with an actual change or a fired trigger.
